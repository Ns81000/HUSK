# Production relay origin, baked into `pnpm run build` (Vite mode production).
# Public URL, not a secret; it is also allowlisted in worker/wrangler.toml.
VITE_WORKER_URL=https://husk.ns8pc1.workers.dev
