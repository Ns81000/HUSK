# Build input for the axe/Playwright a11y harness (pnpm build:a11y).
# Points the frontend at its own origin so the leave-room modal E2E spec can
# intercept create/join/socket calls against the served production build.
VITE_WORKER_URL=http://127.0.0.1:8787
